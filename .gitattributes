# Auto-detect text files and normalize to LF
* text=auto eol=lf

# Explicitly enforce LF for common code files
*.js     text eol=lf
*.json   text eol=lf
*.css    text eol=lf
*.html   text eol=lf
*.md     text eol=lf
*.txt    text eol=lf

# Images and binaries → keep as-is
*.png    binary
*.jpg    binary
*.jpeg   binary
*.svg    binary

# Don't normalize node_modules (they should be gitignored anyway)
node_modules/* -text
